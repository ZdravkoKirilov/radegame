import { FormDefinition, ConnectedEntities } from "@app/dynamic-forms";
import { Stack } from "@app/game-mechanics";

export const composeStackForm: FormDefinition = (data: Stack, ent: ConnectedEntities) => {
    return [];
};