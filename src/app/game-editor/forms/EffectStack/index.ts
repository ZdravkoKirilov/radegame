import { FormDefinition, ConnectedEntities } from "@app/dynamic-forms";
import { EffectStack } from "@app/game-mechanics";

export const composeEffectStackForm: FormDefinition = (data: EffectStack, ent: ConnectedEntities) => {
    return [];
};