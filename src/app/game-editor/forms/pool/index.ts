import { FormDefinition, ConnectedEntities } from "@app/dynamic-forms";
import { Pool } from "@app/game-mechanics";

export const composePoolForm: FormDefinition = (data: Pool, ent: ConnectedEntities) => {
    return [];
}