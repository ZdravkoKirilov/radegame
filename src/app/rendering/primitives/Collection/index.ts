import { BaseProps } from "../../models";
import { BasicComponent } from "../../mixins";

export class PixiCollection extends BasicComponent {

    constructor(props: BaseProps, graphic: any) {
        super(props, graphic);
    }
}